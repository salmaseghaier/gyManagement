{% extends 'base.html.twig' %}

{% block title %}Edit Abonnement{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Edit Abonnement</h1>

        <div class="card shadow-sm p-4 mb-4">
            {{ include('abonnement/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="d-flex justify-content-between mt-3">
            <a class="btn btn-secondary" href="{{ path('app_abonnement_index') }}">Back to List</a>
            {{ include('abonnement/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
