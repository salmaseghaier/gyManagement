<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gym Management System</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Gym Management</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_index') }}">Users</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_abonnement_index') }}">Subscriptions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_course_index') }}">Courses</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <div class="jumbotron text-center">
        <h1 class="display-4">Welcome to the Gym Management System!</h1>
        <p class="lead">Manage your users, subscriptions, and courses efficiently.</p>
        <a class="btn btn-primary btn-lg" href="{{ path('app_abonnement_index') }}" role="button">View Subscriptions</a>
    </div>

    <hr class="my-4">

    <h2>Recent Courses</h2>
    <div class="row">
        {% for course in courses %}
            <div class="col-md-4">
                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">{{ course.title }}</h5>
                        <p class="card-text">{{ course.description|slice(0, 100) ~ '...' }}</p>
                        <p class="card-text"><small class="text-muted">{{ course.schedule|date('F j, Y, g:i a') }}</small></p>
                        <a href="#" class="btn btn-sm btn-outline-primary">View Details</a>
                    </div>
                </div>
            </div>
        {% else %}
            <p>No courses available at the moment.</p>
        {% endfor %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
