{% extends 'base.html.twig' %}

{% block title %}Edit Course{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Edit Course: {{ course.title }}</h1>

        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="card-title">Update Course Information</h5>
            </div>
            <div class="card-body">
                {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.title, 'Course Title') }}
                    {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.description, 'Course Description') }}
                    {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.schedule, 'Course Schedule') }}
                    {{ form_widget(form.schedule, {'attr': {'class': 'form-control'}}) }}
                </div>
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-save"></i> Save changes
                </button>
                {{ form_end(form) }}
            </div>
        </div>

        <a href="{{ path('app_course_index') }}" class="btn btn-secondary mt-3">
            <i class="bi bi-arrow-left"></i> Back to list
        </a>
    </div>
{% endblock %}
